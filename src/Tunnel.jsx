/** Users can only see this component if they are authorized by the API. */
export default function Tunnel() {
  return (
    <section>
      <p>The tablet hums. The gate opens. You step into the tunnel.</p>
    </section>
  );
}
